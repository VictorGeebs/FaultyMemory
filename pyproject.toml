[build-system]
requires = [
    "setuptools",
    "wheel",
]

[tool.isort]
known_first_party = [
    "FaultyMemory",
    "docs",
    "tests",
    "models",
]
profile = "black"
line_length = 120
force_sort_within_sections = "False"
order_by_type = "False"